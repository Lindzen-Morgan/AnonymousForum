@page "/quiz-creation"
@using Microsoft.AspNetCore.Authorization
@attribute [Authorize(Policy = "MustBeLoggedIn")]


<h3>Create a New Quiz</h3>

<!-- Quiz creation form and logic go here -->
@code {
    // Quiz creation logic
}
